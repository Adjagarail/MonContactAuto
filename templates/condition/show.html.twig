{% extends 'base.html.twig' %}

{% block title %}Condition{% endblock %}

{% block body %}
    <h1>Condition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ condition.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ condition.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('condition_index') }}">back to list</a>

    <a href="{{ path('condition_edit', {'id': condition.id}) }}">edit</a>

    {{ include('condition/_delete_form.html.twig') }}
{% endblock %}
