<form method="post" action="{{ path('conexpertise_delete', {'id': conexpertise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conexpertise.id) }}">
    <button class="btn">Delete</button>
</form>
