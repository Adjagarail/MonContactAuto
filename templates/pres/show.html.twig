{% extends 'admin.html.twig' %}

{% block title %}Pres{% endblock %}

{% block body %}
    <h1>Pres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pre.id }}</td>
            </tr>
            <tr>
                <th>Desription</th>
                <td>{{ pre.desription }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pres_index') }}">back to list</a>

    <a href="{{ path('pres_edit', {'id': pre.id}) }}">edit</a>

    {{ include('pres/_delete_form.html.twig') }}
{% endblock %}
