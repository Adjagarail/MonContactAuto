<form method="post" action="{{ path('convoyages_delete', {'id': convoyage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ convoyage.id) }}">
    <button class="btn">Delete</button>
</form>
