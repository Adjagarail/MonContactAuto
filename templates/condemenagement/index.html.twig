{% extends 'base.html.twig' %}

{% block title %}Condemenagement index{% endblock %}

{% block body %}
    <h1>Condemenagement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Contenu</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for condemenagement in condemenagements %}
            <tr>
                <td>{{ condemenagement.id }}</td>
                <td>{{ condemenagement.contenu }}</td>
                <td>
                    <a href="{{ path('condemenagement_show', {'id': condemenagement.id}) }}">show</a>
                    <a href="{{ path('condemenagement_edit', {'id': condemenagement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('condemenagement_new') }}">Create new</a>
{% endblock %}
