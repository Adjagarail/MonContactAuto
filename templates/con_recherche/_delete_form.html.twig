<form method="post" action="{{ path('con_recherche_delete', {'id': con_recherche.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ con_recherche.id) }}">
    <button class="btn">Delete</button>
</form>
