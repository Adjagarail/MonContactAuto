{% extends 'base.html.twig' %}

{% block title %}ConRecherche index{% endblock %}

{% block body %}
    <h1>ConRecherche index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Contenu</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for con_recherche in con_recherches %}
            <tr>
                <td>{{ con_recherche.id }}</td>
                <td>{{ con_recherche.contenu }}</td>
                <td>
                    <a href="{{ path('con_recherche_show', {'id': con_recherche.id}) }}">show</a>
                    <a href="{{ path('con_recherche_edit', {'id': con_recherche.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('con_recherche_new') }}">Create new</a>
{% endblock %}
