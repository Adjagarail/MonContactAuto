{% extends 'base.html.twig' %}

{% block title %}Condconvoyage index{% endblock %}

{% block body %}
    <h1>Condconvoyage index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for condconvoyage in condconvoyages %}
            <tr>
                <td>{{ condconvoyage.id }}</td>
                <td>
                    <a href="{{ path('condconvoyage_show', {'id': condconvoyage.id}) }}">show</a>
                    <a href="{{ path('condconvoyage_edit', {'id': condconvoyage.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('condconvoyage_new') }}">Create new</a>
{% endblock %}
