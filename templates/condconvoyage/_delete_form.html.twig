<form method="post" action="{{ path('condconvoyage_delete', {'id': condconvoyage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ condconvoyage.id) }}">
    <button class="btn">Delete</button>
</form>
