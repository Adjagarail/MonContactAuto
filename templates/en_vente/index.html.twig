{% extends 'base.html.twig' %}

{% block title %}Hello EnVenteController!{% endblock %}

{% block body %}
   <div class="container">
      {{ form_start(formulaire) }}
      {{ form_widget(formulaire) }}
      <button class="btn">{{ button_label|default('Save') }}</button>
      {{ form_end(formulaire) }}

   </div>
{% endblock %}
