{% extends 'base.html.twig' %}

{% block title %}Condeposer index{% endblock %}

{% block body %}
    <h1>Condeposer index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Contenu</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for condeposer in condeposers %}
            <tr>
                <td>{{ condeposer.id }}</td>
                <td>{{ condeposer.contenu }}</td>
                <td>
                    <a href="{{ path('condeposer_show', {'id': condeposer.id}) }}">show</a>
                    <a href="{{ path('condeposer_edit', {'id': condeposer.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('condeposer_new') }}">Create new</a>
{% endblock %}
