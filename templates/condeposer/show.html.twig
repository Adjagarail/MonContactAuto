{% extends 'base.html.twig' %}

{% block title %}Condeposer{% endblock %}

{% block body %}
    <h1>Condeposer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ condeposer.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ condeposer.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('condeposer_index') }}">back to list</a>

    <a href="{{ path('condeposer_edit', {'id': condeposer.id}) }}">edit</a>

    {{ include('condeposer/_delete_form.html.twig') }}
{% endblock %}
