<form method="post" action="{{ path('racheter_delete', {'id': racheter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ racheter.id) }}">
    <button class="btn">Delete</button>
</form>
