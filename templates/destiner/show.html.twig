{% extends 'base.html.twig' %}

{% block title %}Destiner{% endblock %}

{% block body %}
    <h1>Destiner</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ destiner.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ destiner.name }}</td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('destiner_edit', {'id': destiner.id}) }}">edit</a>

    {{ include('destiner/_delete_form.html.twig') }}
{% endblock %}
